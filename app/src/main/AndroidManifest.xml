<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.revampedvision">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RevampedVision">
        <activity android:name=".notf"></activity>
        <activity android:name=".led" />
        <activity android:name=".humidade" />
        <activity android:name=".temperatura" />
        <activity android:name=".iluminacao" />
        <activity android:name=".Menu" />
        <activity android:name=".Reg" />
        <activity android:name=".LoginUti" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>